  <list>
    <list-item a:for="{{messages}}" key="k" thumb="{{baseUrl+item.avator}}"
    upperSubtitle="{{item.content}}" 
    lowerSubtitle="{{item.time}}"
    >
    {{item.name}}
  </list-item>
  </list>